# Build output
output/
tmp/
*.o

# NuGet packaging: the static library and the manifest are copied into the
# package project by build.sh / CI, never committed.
src/runtime/runtimes/
src/runtime/*.bflat.manifest
artifacts/
bin/
obj/
